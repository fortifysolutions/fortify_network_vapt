def run(target, verbose=False):
    return {
        "raw": {"note": "Risk scoring is computed centrally in reporter from module outputs."},
        "parsed": {"status": "computed_in_reporter"}
    }
